import ScrapedContent from '../src/models/scrapedContent';
import * as assert from 'assert';

describe('content test', function() {
    it('Can create an object', function() {
        const sc = new ScrapedContent()
    })

});
